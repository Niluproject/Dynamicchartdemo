import React, { Suspense, lazy } from 'react'
// const Nilesh = lazy(()=>import('./Niesh'))
function Lazyload() {
    return (
        <div>
            <h1>Lazy Loading</h1>
        </div>
    )
}

export default Lazyload
